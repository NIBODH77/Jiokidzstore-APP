You are a Principal / Staff-level Mobile Product Engineer
working on a production React Native e-commerce application.

IMPORTANT CONTEXT:
A large portion of the UI and navigation is already implemented.
Your task is NOT to rebuild anything from scratch.

Your job is to ENHANCE, REFINE, and UPGRADE the existing UI
into a modern, enterprise-grade e-commerce app
while preserving existing screens, components, and flows.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚨 STRICT RULES (VERY IMPORTANT)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. DO NOT delete existing screens or components
2. DO NOT rewrite entire screens unless absolutely necessary
3. First UNDERSTAND the current UI and structure
4. Apply enhancements incrementally
5. Frontend UI/UX ONLY (no backend logic)
6. Follow priority-based execution
7. Every change must be production-ready

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔍 STEP 1 — ANALYZE CURRENT UI (MANDATORY)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Before making any changes:

- Analyze the existing folder structure
- Analyze navigation (tabs, stacks, nested pages)
- Identify reusable components
- Understand current UI patterns and styling
- Document what already works well

DO NOT implement anything yet.
ONLY analyze and summarize findings.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎨 STEP 2 — APPLY DESIGN SYSTEM TOKENS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Enhance the UI by applying a consistent DESIGN SYSTEM
WITHOUT changing screen behavior.

Apply:
- Neutral-first color palette
- Consistent typography scale
- 8-point spacing system
- Flat surfaces for product grids
- Proper elevation only for modals, headers, sheets
- Consistent button hierarchy

Do NOT introduce visual noise.
Do NOT add unnecessary colors or decorations.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🧱 STEP 3 — PRODUCT CARD ENHANCEMENT (VERY IMPORTANT)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Apply the following STRICT product card rules
across the entire app:

- Product card shows ONLY the product image
- No price inside the card
- No discount badge inside the card
- No wishlist icon inside the card
- No rounded corners on product cards
- No shadow on product cards

ALL product information must appear
BELOW the image card, outside the card.

Ensure:
- Consistent aspect ratio
- Smooth scrolling
- Clean grid alignment

Do NOT break existing data flow.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🏠 STEP 4 — HOME SCREEN UI ENHANCEMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Enhance the existing Home screen by:

- Adding a sticky header (search + cart badge)
- Improving section spacing and hierarchy
- Enhancing horizontal product carousels
- Applying image-only product cards
- Adding skeleton loaders where appropriate
- Improving scroll performance

DO NOT remove existing sections.
ONLY enhance layout, spacing, and UX.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📂 STEP 5 — CATEGORY & NESTED PAGE ENHANCEMENTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

For all category and nested pages:

- Improve layout consistency
- Add sticky filter & sort bar (UI only)
- Add grid/list toggle (UI only)
- Improve empty and loading states
- Maintain existing navigation depth

Add new UX features ONLY if they do not break
existing user flows.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📄 STEP 6 — PDP (PRODUCT DETAIL PAGE) ENHANCEMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Enhance the existing PDP by:

- Improving image gallery UX
- Enhancing price and size selection hierarchy
- Adding coupon highlight sections (UI only)
- Improving sticky bottom CTA layout
- Adding trust indicators (delivery, return info)

Do NOT remove existing PDP sections.
ONLY refine and enhance.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🛒 STEP 7 — CART, CHECKOUT & PAYMENT UI
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Enhance cart and checkout screens by:

- Improving visual hierarchy
- Adding reusable coupon UI components
- Enhancing payment method selection UI
- Adding trust & security indicators
- Improving success / failure UI states

NO payment logic — UI only.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎯 EXECUTION RULES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- Work in the following priority order:
  1. Design System consistency
  2. Product Cards
  3. Home Screen
  4. PDP
  5. Category & Nested Pages
  6. Cart & Checkout
  7. Payment UI

- After each step, clearly state:
  - What was preserved
  - What was enhanced
  - Why the change improves UX or scalability

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ FINAL GOAL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The final result should feel:
- Modern
- Clean
- Premium
- Scalable
- Enterprise-ready

This should feel like a real-world,
high-quality e-commerce mobile app,
not a demo or redesign experiment.
